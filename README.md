# agentic-feature-traced-datasets
Collection of datasets of agentic/vibe-coded software with embedded traceability annotations. Holds no content itself — submodule pointers to each dataset repo + governance docs (README, LICENSE, .gitmodules).
